<html>
    <body>
        <p style="color: #47476b; text-align: center; font-size: 150%; font-weight: bold">MyWebsite</p>
        <hr>
        <p>Hello <span style="color: #47476b; font-size: 110%;">{{ recipient.username }}</span>,</p>
        <br/>
        <p>You have requested to reset your password, please visit the following link to proceed:</p>
        <br>
        <p><a href="{{ url('password_reset', {token: token.publicToken}) }}" target="_blank">{{ url('password_reset', {token: token.publicToken}) }}</a></p>
        <br>
        <p>This link will expire in 1 hour ({{ token.expiredAt|date("Y-m-d \\a\\t H:i") }}).</p>
        <br>
        <p style="font-style: italic;">Click <a href="{{ url('login') }}" target="_blank">here</a> to go to MyWebsite.</p>
    </body>
</html>